/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = {
  swcMinify: true
}

module.exports = {
  i18n: {
    locales: ["vi","en"],
    defaultLocale: "en",
  },
};